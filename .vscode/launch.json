{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run with Docker",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/runner.js",
            "args": [
                "${fileBasename}"
            ],
            "console": "integratedTerminal",
            "preLaunchTask": "Build with Docker"
        }
    ]
}